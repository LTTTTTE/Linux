포크 -> 포크 호출한 프로세스를 똑같이 복제해서 메인메모리에 올린다.
만든 프로세스는 부모프로세스라고 말하고
만들어진 프로세스는 자식 프로세스이다.

결국 병렬적으로 프로세스가 돌아간다.

exec -> execl("실행파일", args...)
exec호출 한 프로세스는 실행파일 실행이되며 프로세스는 대치된다.

시스템 부팅도 fork/exec 시스템 호출을 통해서 이루어진다

(9-40)
swapper -> init(1) -> getty -> login -> shell

데몬 >> & 붙이면댐